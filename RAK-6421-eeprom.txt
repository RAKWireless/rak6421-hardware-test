########################################################################
# EEPROM settings text file for RAK6421 WisBlock Pi HAT
#
# Edit this file for your particular board and run through eepmake tool,
# then use eepflash tool to write to attached HAT ID EEPROM 
#
# Tools available:
#  eepmake   Parses EEPROM text file and creates binary .eep file
#  eepdump   Dumps a binary .eep file as human readable text (for debug)
#  eepflash  Write or read .eep binary image to/from HAT EEPROM
#
# Usage:
#   1. Copy this template file for each device
#   2. Edit the configuration values below
#   3. Run: eepmake <filename>.txt <filename>.eep
#   4. Run: eepflash.sh -w -f=<filename>.eep -t=24c32
#
########################################################################

########################################################################
# Basic Product Information
########################################################################

# 128 bit UUID. If left at zero eepmake tool will auto-generate
# RFC 4122 compliant UUID
product_uuid 00000000-0000-0000-0000-000000000000

# 16 bit product id
product_id 0x6421

# 16 bit product version
product_ver 0x0002

# ASCII vendor string  (max 255 characters)
vendor "RAK"

# ASCII product string (max 255 characters)
product "6421 Pi Hat"

# How much current the HAT+ can supply, in milliamps
current_supply 2000

# Which Device Tree overlay to load
dt_blob "none,i2c_arm=on,spi=on"

########################################################################
# RAK6421 Hardware Production Configuration
# Each custom_data field stores specific configuration information
# Format: custom_data "field_name value"
# IMPORTANT: Use space to separate field name and value, NOT colon
########################################################################

# Hardware version/revision (e.g. A, B, C, D, etc)
custom_data "hardware_version D"

# IO Slot 1 module
# Options: RAK13300, RAK13302, RAK5801 (4-20mA), RAK5802 (RS485), 0 (empty) and others
# custom_data "io_slot1 RAK13302"

# IO Slot 2 module
# Options: Same as IO Slot 1
# custom_data "io_slot2 0"

# Sensor Slot A module
# Options: RAK1901 (Temp/Humidity), RAK1902 (Pressure), 0 (empty) and others
# custom_data "sensor_slot_a RAK1901"

# Sensor Slot B module
# Options: Same as Sensor Slot A
# custom_data "sensor_slot_b 0"

# Sensor Slot C module
# Options: Same as Sensor Slot A
# custom_data "sensor_slot_c 0"

# Sensor Slot D module
# Options: Same as Sensor Slot A
# custom_data "sensor_slot_d 0"

# Serial number (18 characters)
# Format: Use space separator, e.g. "serial 012345678910111213"
# custom_data "S/N 918100000000000000"

# Additional custom data (MO number, batch number, notes, etc)
# Format: Use space separator, 12 characters for MO number
# custom_data "MO_number 000000000000"
